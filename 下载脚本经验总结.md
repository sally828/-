# 书目自动下载脚本 · 经验总结

> 记录这套脚本最终能跑通的原因，以及踩过的坑，方便日后针对新网站复用。

---

## 一、最终成功的架构（三步走）

```
fast_download URL（需登录）
        ↓  浏览器自动跳转
    CDN URL（公开，不需要 Cookie）
        ↓  Python requests 流式下载
      本地文件
```

### 核心代码逻辑

```python
# 第一步：让浏览器帮我们处理登录验证和跳转
await page.goto(fast_download_url, wait_until="commit", timeout=30000)

# 第二步：轮询地址栏，等 CDN 地址出现（最多 12 秒）
cdn_url = fast_download_url
for _ in range(12):
    await asyncio.sleep(1)
    current = page.url
    if current and "原始网站域名" not in current and current != fast_download_url:
        cdn_url = current
        break

# 第三步：requests 在线程里流式下载，字节直接写盘
# asyncio.to_thread 防止阻塞 Playwright 事件循环
sp = await asyncio.to_thread(_do_download, cdn_url, DOWNLOAD_DIR, 文件名, proxies)
```

### `_do_download` 同步函数（关键）

```python
def _do_download(cdn_url, save_dir, base_name, proxies):
    r = requests.get(cdn_url, headers=HEADERS, proxies=proxies, timeout=120, stream=True)
    r.raise_for_status()
    ct  = r.headers.get("content-type", "").lower()
    ext = 根据 ct 判断扩展名  # pdf / epub / mobi 等
    sp  = save_dir / f"{base_name}.{ext}"
    with open(sp, "wb") as f:
        for chunk in r.iter_content(chunk_size=65536):  # 64KB 分块
            if chunk:
                f.write(chunk)
    return sp
```

---

## 二、为什么选这个方案

### 关键决策 1：用 `requests` 而不是 `page.context.request.get()`

`page.context.request.get()` 会把文件字节从 Playwright 的 Node.js 进程经 IPC Socket 传回 Python。  
**大文件（约 100 MB+）会把这个 Socket 撑爆**，抛出：
```
UNKNOWN: unknown error, write
APIRequestContext.get: Connection closed while reading from the driver
```
Socket 崩掉之后，后续所有书都无法下载。

`requests` 直连 CDN，字节流直接写盘，完全不经过 Playwright，无论多大的文件都没问题。

### 关键决策 2：`asyncio.to_thread` 跑同步下载

`requests` 是同步库，直接在 `async` 函数里调用会阻塞整个事件循环，导致 Playwright 的心跳超时。  
`asyncio.to_thread(fn, *args)` 把同步函数扔到线程池里跑，主循环不阻塞。

### 关键决策 3：`wait_until="commit"` 而非 `"load"`

跳转到 CDN 后 Chrome 会直接显示 PDF（不是 HTML 页面），`"load"` 事件可能永远不触发。  
`"commit"` 只要 URL 确认跳转就立刻返回，之后再用轮询等 CDN URL 出现。

### 关键决策 4：断点续传（`downloaded.txt`）

每成功下载一本就把序号追加写入 `downloaded.txt`。  
重新运行时跳过已有序号，不会重复下载，中途中断可以直接续跑。

---

## 三、踩过的坑（别再走）

| 方案 | 失败原因 |
|------|---------|
| `expect_download + page.goto()` | Anna's Archive 的 fast_download 跳转到 PDF 查看器，不触发 Playwright 的 download 事件 |
| `--disable-pdf-viewer` Chrome 参数 | 新版 Chromium 已忽略此参数，PDF 依然在浏览器内打开 |
| `page.on("response") + response.body()` | Chrome 内部渲染 PDF 时，Playwright 无法读取响应体（body 为空） |
| `page.route() + route.fetch()` | Chrome 对 PDF 导航有内部处理流程，路由拦截抢不到字节 |
| `shadow_locator("#download")` 点 PDF 查看器下载按钮 | 旧版 Playwright 没有 `shadow_locator` 方法；且 Chrome 内置 PDF 查看器的 shadow DOM 不一定可访问 |
| `page.context.request.get()` | 大文件经 IPC 传输崩溃（见上） |
| 直接用 requests + API Key 下载 | 库的代理没配好；API 返回的不是文件而是 HTML；认证方式不匹配 |

---

## 四、针对新网站的复用指南

### 前提条件（必须满足）

1. 网站有**固定的书目清单**（有序号、书名、下载链接三列即可）
2. 下载链接点进去**会跳转到 CDN 直链**（地址栏变成另一个域名）
3. CDN 直链是**可以直接 GET 的文件**（不是又套了一层 JS 跳转）

### 改动清单

**`书目搜索结果.xlsx` → 新的书单文件**

修改 `load_books()` 函数，对应新文件的列名：
```python
num  = str(data.get("序号", "") or "").strip()
name = str(data.get("找到书名", "") or data.get("原书名", "") or "").strip()
url  = str(data.get("下载链接", "") or "").strip()
```

**`BASE_URL` → 新网站的域名**

```python
BASE_URL = "https://新网站.com"
```

**登录等待逻辑 → 不变**（通用）

脚本打开 `/account` 或登录页，等你手动登录后按 Enter，后续全自动。

**等 CDN URL 出现的判断条件 → 根据新网站调整**

```python
# 把 "annas-archive" 换成新网站的域名关键字
if current and "新网站域名关键字" not in current and current != url:
    cdn_url = current
    break
```

**`PROXY` → 按实际情况**

```python
PROXY = "http://127.0.0.1:10808"  # V2RayN 系统代理端口
# PROXY = None                    # 不用代理
```

---

## 五、文件结构

```
书目搜索/
├── auto_download.py        # 主下载脚本
├── search_anna.py          # 搜索脚本（生成 Excel）
├── convert_to_csv.py       # 文本书单 → CSV
├── update_catalog.py       # 搜索结果写回 CSV
├── 书目搜索结果.xlsx        # 搜索结果（含下载链接）
├── books_input.csv         # 输入书单
├── downloaded.txt          # 已下载记录（断点续传）
├── 下载书籍/               # 文件保存目录（自动创建）
└── 使用说明.txt
```

---

## 六、运行顺序

```
# 1. 准备书单
python convert_to_csv.py       # 文本 → books_input.csv（如果还没做）

# 2. 搜索书籍（获取下载链接）
python search_anna.py          # 生成 书目搜索结果.xlsx

# 3. 下载书籍
python auto_download.py        # 打开浏览器 → 登录 → 按 Enter → 全自动

# 4. （可选）更新书目状态
python update_catalog.py       # 把搜索结果写回 books_input.csv
```

---

## 七、常见问题

**Q：下载速度很慢？**  
A：每本书之间有 1.5~3 秒随机等待（避免被封），属于正常现象。下载本身是满速的。

**Q：中途断掉怎么办？**  
A：直接重新运行 `python auto_download.py`，会自动跳过 `downloaded.txt` 里的已完成书目。

**Q：某本书一直失败？**  
A：可能是 CDN 链接失效或该书格式特殊。手动在浏览器里打开 Excel 里的链接确认一下。

**Q：代理连不上？**  
A：确认 V2RayN 开着，模式选"系统代理"（不是 TUN 模式），端口 10808。`PROXY` 变量设为 `"http://127.0.0.1:10808"`。

**Q：`requests` 未安装？**  
A：`pip install requests`
